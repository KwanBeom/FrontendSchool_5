// 반복문에서는 사용자의 개입으로 break를 사용해 반복문에서 탈출할 수 있습니다.
// 반복문이 종료되면 그 다음 코드가 차례대로 실행 됨
// break은 자신을 감싼 반복문만 탈출


// continue는 아래 구문을 실행시키지 않고 다음 루프로 넘어가서 다시 구문을 실행

// label은 특정 코드 블록에 이름을 지정하는 방법이다.
// 가독성을 해치기 때문에 지양하길 권장
outer : for(반복문;;){};